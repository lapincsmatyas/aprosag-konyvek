stages:
  - dependencies
  - test
  - build

install_dependencies:
  image: node:12-alpine
  stage: dependencies
  script:
    - npm install
  artifacts:
    paths:
      - node_modules

lint:
  image: node:12-alpine
  stage: test
  dependencies:
    - install_dependencies
  script:
    - npm link @angular/cli
    - ng lint

build_image:
  image: node:12-alpine
  stage: build
  dependencies:
    - install_dependencies
  script:
    - npm link @angular/cli
    - npm run build
  artifacts:
    paths:
      - $CI_PROJECT_DIR/dist
